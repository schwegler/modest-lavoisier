🎯 **What:**
Fix Electron E2E tests for linux CI (GitHub Actions)

📊 **Coverage:**
The tests in `tests/electron.spec.js` were modified to handle:
- Adding env var PLAYWRIGHT_TEST to distinguish it
- Conditionally asserting left padding that assumes process.platform darwin to ensure standard styling behaves correctly based on platform
- Wait for elements asynchronously so that headless linux tests have the split-layout visible before clicking elements.

✨ **Result:**
All 22 Playwright tests pass accurately across platforms, repairing the Github CI test breakage without regressing electron functionality testing.
